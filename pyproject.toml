[project]
name = "snemu-tools"
version = "0"
description = "Lint tooling for snemu (C/C++ DPDK PMD)"
requires-python = ">=3.10"
dependencies = [
    "clang-format==22.1.4",
    "clang-tidy==22.1.0.1",
]

[tool.uv]
package = false
